name: instagram_ui_clone
description: monfauzi.com.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.7.0
  marquee: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0  

flutter:
  uses-material-design: true
  assets:
    - assets/logo/
    - assets/avatars/
    - assets/feeds/
    - assets/icons/
    - assets/search/
